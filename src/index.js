import React from "react"; // Importing the 'react' library to create React components.
import ReactDOM from "react-dom"; // Importing 'react-dom' library for rendering components to the DOM.

import App from "./App"; // Importing the main 'App' component from './App.js' file.

// Rendering the 'App' component to the root element in the HTML document.
// The 'App' component will be the starting point of the React application.
ReactDOM.render(<App />, document.getElementById("root"));


// ----------------------------------------------------------------------**************************************************************************************************

// import React from "react";
// import ReactDOM from "react-dom";

// import App from "./App";

// ReactDOM.render(<App />, document.getElementById("root"));


